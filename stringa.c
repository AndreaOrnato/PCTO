#include<stdio.h>
void Array()
{
    int array[5]={5,6,8,2,42};                 //gli indici partono dallo 0 che in questo caso corriponde a 5.
                 //0,1,2,3,4...                //array è un nome casuale
    
    printf("%d\n",array[3]);                   //in questo caso per selezionare l'indiceda printare dobbiamo mettere i numero al quale corrisponde nella sequenza
    int array[1]=18;
    printf("%D\n",array[1]);                

    return(0);
}
void Stringa()
{
    char stringa[5]={'c','i','a','o', '\0'};    //quando creiamo un array di char dobbiamo inserire all'ultimo carattere in sequenza dell'array '\0' ovvero l'ultimo carattere della tabella ashi
    int c = 5;
    char stringa2[]="ciao";
}
int main()
{
    N();
    
    return(0);
}
